{
  "description": "Add admin_activity_logs_id and admin_diagnostic_sessions_id FK columns to payload_locked_documents_rels",
  "changes": {
    "payload_locked_documents_rels": {
      "added_columns": [
        "admin_activity_logs_id",
        "admin_diagnostic_sessions_id"
      ]
    }
  }
}
